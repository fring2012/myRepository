<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.c.appdemo">

    <!-- 权限设置 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- 登录界面 -->
        <activity android:name="com.example.c.ui.activity.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 版本信息和下载界面 -->
        <activity android:name="com.example.c.ui.activity.activity.VersionManagerActivity">

        </activity>

        <!-- 注册界面 -->
        <activity android:name="com.example.c.ui.activity.activity.RegisterActivity">

        </activity>
        <!-- 登录服务 -->
        <service android:name="com.example.c.service.LoginService">

        </service>
        <!-- 版本信息和下载服务 -->
        <service android:name="com.example.c.service.VersionService">

        </service>
    </application>
</manifest>